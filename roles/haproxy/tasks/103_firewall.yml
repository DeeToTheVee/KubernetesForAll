---

- name: "[Firewall]: Enable firewall rules"
  firewalld:
    port: "{{item}}"
    permanent: yes
    state: enabled
    zone: public
  with_items:
    - 6443/tcp
    - 80/tcp
  tags:
    - install_haproxy