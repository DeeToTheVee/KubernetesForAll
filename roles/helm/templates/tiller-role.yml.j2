---

apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: "{{item}}"
  name: tiller-manager
rules:
  - apiGroups: ["", "batch", "extensions", "apps"]
    resources: ["*"]
    verbs: ["*"]